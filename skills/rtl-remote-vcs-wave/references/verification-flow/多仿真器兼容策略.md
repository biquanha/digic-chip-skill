# 多仿真器兼容策略

如果机器上存在 `xrun/irun` 或 `qverilog/vsim`，可考虑让平台支持多仿真器兼容。

## 适用场景
- 团队内部使用多种仿真器
- 需要验证 testbench 可移植性
- 外部合作方使用不同工具链

## 接入建议
- 把仿真器相关选项从 Makefile 中参数化
- 保持 testcase 与回归入口尽量一致
- 先保证主仿真器稳定，再扩展兼容层
